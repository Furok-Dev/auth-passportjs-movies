/**
 * En este archivo estableceremos los tiempos
 * para que el manejador de cacche funcione en
 * nuestra aplicacion
 */

const FIVE_MINUTES_IN_SECONDS = 300;
const SIXTY_MINUTES_IN_SECONDS = 3600;

module.exports = {
  FIVE_MINUTES_IN_SECONDS,
  SIXTY_MINUTES_IN_SECONDS,
};
